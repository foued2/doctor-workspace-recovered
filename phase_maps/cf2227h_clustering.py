"""phase_maps.cf2227h_clustering — stub.

Reconstructed from import map. `_distance` is used in a `min(...)`
key function; returns a comparable distance value.
"""
from __future__ import annotations

from typing import Any


def _distance(a: Any, b: Any) -> float:
    return 0.0
