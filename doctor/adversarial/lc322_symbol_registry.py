"""LC322 symbol registry — reconstructed from import map analysis.

Reconstructs the public surface imported by 11 files in doctor/adversarial/.
Symbols exposed:
  - LC322_SYMBOL_REGISTRY

Shape is unknown — opaque registry passed to `assert_valid_schema(LC322,
registry=...)` only. No caller dereferences its contents. Defined as an
empty dict so import succeeds and the kwarg is satisfied.
"""
from __future__ import annotations

LC322_SYMBOL_REGISTRY: dict = {}
