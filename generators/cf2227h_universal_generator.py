"""CF2227H universal generator — stub.

Reconstructed from import map. `generate_inputs` is called with
`seed=`, `small_per_n=`, `large_count=` kwargs; no records generated.
`random_tree(n, rng)` is called with `n` (int) and `rng` (random.Random)
and its return value is assigned to `edges`; no tree is built.
"""
from __future__ import annotations

import random

_PROBLEM_ID = "cf2227h"


def generate_inputs(*, seed: int = 0, small_per_n: int = 10, large_count: int = 100, **kwargs) -> list[dict]:
    return []


def random_tree(n: int, rng: random.Random) -> list:
    return []
