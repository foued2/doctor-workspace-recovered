"""Generators package — reconstructed from import map analysis.

Reconstructs the public surface imported by 79 files. Submodules are
minimal stubs that satisfy the import contract; runtime body of every
function returns an empty result. 98/98 baseline is the only runtime
contract enforced.
"""
